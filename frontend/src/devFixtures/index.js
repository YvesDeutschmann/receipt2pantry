/**
 * Development-only receipt fixtures (mirrors data/fixtures/*.json).
 * Used by DEV mock sync buttons; do not import from production code paths.
 */
import safewayFixtures from './safeway_receipts.json'
import costcoFixtures from './costco_receipts.json'

export { safewayFixtures, costcoFixtures }
